set clipboard=unnamedplus
inoremap jj <Esc>
set shiftwidth=2    " 按 >/< 时的缩进空格数（默认8）
set tabstop=2       " 文件中一个Tab显示的宽度（需与shiftwidth一致）
set softtabstop=2   " 按Tab键时插入的空格数（若为0则用tabstop值）
set expandtab       " 将Tab转换为空格（推荐）
